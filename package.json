{
  "name": "mss-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:api": "cd services/api && .venv/bin/uvicorn main:app --reload --port 8000",
    "dev:empire": "cd apps/hora && npm run dev",
    "seed": "cd services/api && python -m scripts.seed",
    "dev:7777": "cd apps/os-prototype && npm run dev"
  },
  "pnpm": {
    "overrides": {
      "@xmldom/xmldom": "^0.8.13",
      "fast-xml-parser": "^5.7.0",
      "protocol-buffers-schema": "^3.6.1"
    }
  }
}
