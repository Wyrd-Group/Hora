[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"

# SPA fallback — must come AFTER function routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
