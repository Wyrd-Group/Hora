fastapi==0.111.0
uvicorn[standard]==0.29.0
asyncpg==0.29.0
redis[hiredis]==5.0.4
httpx==0.27.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.2.2

# ML / Analytics
scikit-learn==1.5.2
h3
numpy==1.26.4
pyod
shap>=0.45.0

# NLP — FinBERT (optional: install transformers+torch for local inference)
# pip install transformers torch  # for local model (2GB download)
# Or just use HF Inference API (no local install needed, set HF_TOKEN env var)

# Forecasting — Chronos-2 foundation model (optional: install for ML forecasts)
# pip install torch chronos-forecasting  # ~200MB for bolt-small model weights
# Without these, falls back to Holt-Winters (statsmodels) or EWMA (zero deps)
statsmodels>=0.14.0

# DRL Training — FinRL via Stable Baselines 3 (optional: install for agent training)
# pip install torch stable-baselines3 gymnasium  # PPO/A2C/SAC training
# Without these, training endpoints return 503; inference via TFJS still works
gymnasium>=1.0.0

# Portfolio optimization — Riskfolio-Lib (pulls cvxpy, scipy, pandas)
# MV, CVaR, MAD, CDaR, HRP, efficient frontier
# Without riskfolio, falls back to scipy SLSQP for basic MV
pandas>=3.0.2
riskfolio-lib>=6.0.0

# Data pipeline
yfinance>=0.2.40
